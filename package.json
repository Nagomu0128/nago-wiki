{
  "name": "nago-wiki",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "check": "npm run lint && npm run typecheck && npm run test && npm run build",
    "deploy:production": "npm run build -w @nago-wiki/web && wrangler deploy --config apps/worker/wrangler.jsonc --env production",
    "dev": "concurrently -n worker,web -c cyan,magenta \"npm run dev -w @nago-wiki/worker\" \"npm run dev -w @nago-wiki/web\"",
    "lint": "eslint . --max-warnings=0",
    "test": "npm run test --workspaces --if-present && npm run test:portable-export-validator",
    "test:portable-export-validator": "node --experimental-strip-types --test scripts/validate-portable-export.test.ts",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "validate:portable-export": "node --experimental-strip-types scripts/validate-portable-export.ts"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.21.3",
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.2.0",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "concurrently": "^10.0.5",
    "eslint": "^10.8.1",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.4",
    "fflate": "^0.8.3",
    "happy-dom": "^20.11.2",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.67.0",
    "vitest": "^4.1.10",
    "wrangler": "^4.123.0"
  }
}
